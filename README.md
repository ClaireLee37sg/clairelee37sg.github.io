# Claire Lee Personal Website

Ready for GitHub Pages.

## Upload files

Upload these files to the repository root:

- `index.html`
- `style.css`
- `claire-lee.png`

For the GitHub username `ClaireLee37sg`, name the repository `clairelee37sg.github.io`.

Then open **Settings → Pages**, choose **Deploy from a branch**, select `main` and `/(root)`, and click **Save**.
